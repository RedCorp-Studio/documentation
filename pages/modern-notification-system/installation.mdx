# Installation

First, add `ensure red_notifications` in your `server.cfg` file. Please make sure you place it before all ressources that might use Modern Notification System.

Finally run the ressource by typing the following commands in your server console:
```
cfx> refresh
cfx> ensure red_notifications
```